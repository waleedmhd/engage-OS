#!/usr/bin/env bash
# Seed reference data (predefined tags, default admin, etc.).
# Phase 0: no-op. Replace with `python -m app.scripts.seed` once seeding logic exists.
set -euo pipefail
echo "seed.sh: no seed data defined yet (Phase 0)"
