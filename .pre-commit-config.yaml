repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.12
    hooks:
      - id: ruff
        args: [--fix]
        name: ruff (lint + autofix)
      - id: ruff-format
        name: ruff (format)
